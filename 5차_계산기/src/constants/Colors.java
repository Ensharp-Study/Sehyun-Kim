package constants;

import java.awt.Color;

public interface Colors {
    Color EQUALS_BUTTON = new Color(0, 153, 153);
    Color BUTTON_TEXT_DEFAULT = Color.BLACK;
    Color BUTTON_BACKGROUND_DEFAULT = Color.WHITE;
    Color BUTTON_TEXT_EQUALS = Color.WHITE;
    Color ERROR_TEXT = Color.RED;
}
